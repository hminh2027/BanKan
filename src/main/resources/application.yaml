spring:
  datasource:
    url: jdbc:mysql://localhost:3306/BanKan
    username: root
    password: 85273200
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect